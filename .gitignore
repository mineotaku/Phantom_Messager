# ── Build Outputs ──
*.iml
.gradle/
.kotlin/
/build/
app/build/
/captures/

# ── IDE ──
.idea/
*.iml

# ── Local Configuration ──
local.properties

# ── Secrets & Credentials ──
.env
debug.keystore
*.jks
*.keystore

# ── Native Build ──
.externalNativeBuild/
.cxx/

# ── OS Artifacts ──
.DS_Store
Thumbs.db

# ── Python Artifacts ──
__pycache__/
*.pyc
*.pyo

# ── Database Dumps ──
*.sqlite

# ── Development Scratch ──
archive/
files/
docs/
metadata.json

# ── Node.js (prevent accidental commits) ──
node_modules/
package-lock.json

# ── Heap / Profiling ──
*.hprof
*.log
